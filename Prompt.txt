I run claude agent in `C:\Docs\Repo\github\pipesim\pip-pipesim\Stingray\`. It does not see harness control flow at all. Fix it


Do this way to give a fix. You should give detailed guide in skills to guide agent how to call the tools such as mcp and `c:\Docs\Hackthon\2026\harness_controller\Scripts\invoke-harness-control-plane.ps1`
The way how you can figure out the fixes needed:

Assume you are a planner agent in `C:\Docs\Repo\github\pipesim\pip-pipesim\Stingray\`. You will do the harness control planning. You read all skills and call harness for the flow

The planner agent plans this:
```
You are in plan mode. All plans should be written to this folder: `C:\Users\SXu12\OneDrive - SLB\AI_plan\1803343\hackthon2026\`. Give a meaningful name of the plan and it should start with an index in two digits.

1. Change the fluid name
2. Switch tab to trigger the change.
3. The name is changed and in the tree dialog, the relevant fluid name is changed. 
4. In reservoir mapping dialog, the grid shows fluid name. But this fluid name does not change. This is wrong

Plan
5. Check why reservoir mapping dialog does not get the change. Plan a fix for it
6. Check this change. You should follow the same pattern as this change. https://dev.azure.com/slb-swt/Flow_Assurance/_git/pip-pipesim/commit/666fb42c89184768cc2c2ea994bfcb2204dd1689

Plan the change and tests
```

In the harness control, planner agent will find bugs and be blocked. Now a bug fixing agent can fix them in `c:\Docs\Hackthon\2026\harness_controller\`. 
When planner agent gets any problem, the bug fixing agent should fix it. the bug fixing agent should place the fix in `c:\Docs\Hackthon\2026\harness_controller\` or subfolders to record what has been imrpoved.

Then planner agent continues the flow until the plan is made

In the end, the bug fixing agent should give a consolidated fix so that the flow for the planner does not block

