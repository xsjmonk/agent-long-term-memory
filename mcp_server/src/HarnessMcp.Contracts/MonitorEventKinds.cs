namespace HarnessMcp.Contracts;

public static class MonitorEventKinds
{
    // Legacy string constants were replaced by MonitorEventKind enum.
    // Keep this type for backward source compatibility, but do not add new usages.
}
